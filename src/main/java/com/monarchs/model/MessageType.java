package com.monarchs.model;

/**
 * Message type enum representing the type of chat message.
 */
public enum MessageType {
    /**
     * Room message - sent to all users in a room
     */
    ROOM,
    
    /**
     * Private message - sent to a specific user
     */
    PRIVATE
}

